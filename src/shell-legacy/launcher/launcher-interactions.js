// Launcher interactivity placeholder
// Search typing, category selection, app hover glow.

export function handleLauncherSearchInput(value) {
  // Placeholder: filter app grid based on search text.
  return value;
}

export function handleLauncherCategorySelect(categoryKey) {
  // Placeholder: switch active category.
  return categoryKey;
}

export function handleLauncherAppHoverStart(appId) {
  // Placeholder: instant-on glow for hovered app tile.
  return appId;
}

export function handleLauncherAppHoverEnd(appId) {
  // Placeholder: smooth fade-out of glow.
  return appId;
}
