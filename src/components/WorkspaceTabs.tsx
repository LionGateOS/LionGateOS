import React from "react";

/**
 * Legacy component kept for compatibility.
 * NOTE: The PRIMARY workspace tabs are now rendered in the TOP BAR via WorkspaceSwitcher (Topbar.tsx).
 * This component intentionally renders nothing in STEP 1.
 */
export default function WorkspaceTabs(): JSX.Element | null {
  return null;
}
